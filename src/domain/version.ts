/**
 * 매치 코드에 결속되는 버전 좌표.
 *
 * 매치 코드는 이 두 값을 그대로 담고, 재생은 코드에 적힌 버전이 현재 버전과
 * 같을 때만 허용된다. 그래서 엔진이나 데이터가 바뀌면 옛 코드는 조용히 다른
 * 경기로 재생되는 대신 명시적으로 거부된다. 규칙이나 계수를 바꿀 때는
 * ENGINE_VERSION을, 시나리오나 스쿼드 사실을 바꿀 때는 DATA_VERSION을 올린다.
 *
 * 값에 마침표를 쓰지 않는다. 매치 코드가 마침표로 조각을 나누므로 버전에
 * 마침표가 들어가면 화면에 노출되는 코드가 퍼센트 인코딩으로 지저분해진다.
 */
export const ENGINE_VERSION = "e2";
export const DATA_VERSION = "d1";
